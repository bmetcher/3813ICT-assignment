export class Group {
    constructor(id, name, admins, members, channels, open) {
        this.id = id,
        this.name = name,
        this.admins = admins,
        this.members = members,
        this.channels = channels,
        this.open = open
    }
}